export const publicRoutes = ["/", "/faq", "/auth/new-verification"]

export const authRoutes = ["/", "/register", "/error", "/reset", "/new-password"]

export const apiAuthPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/dashboard"
